# ==============================================================================
# SECURITY WARNING: NEVER COMMIT ACTUAL VALUES TO VERSION CONTROL!
# ==============================================================================
# This file shows the structure of required environment variables.
# Copy this file to .env and fill in your actual values.
# The .env file should NEVER be committed to git.
#
# FOR PRODUCTION: Set these values in your deployment platform (e.g., Render.com)
# ==============================================================================

# --- Security ---
# Generate a secure key: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=replace_me_with_a_long_random_string_NEVER_COMMIT_REAL_KEY
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# --- Database ---
DATABASE_URL=postgresql+psycopg2://user:password@localhost:5432/waardhaven

# --- Admin ---
ADMIN_TOKEN=replace_with_manual_task_token

# --- Index Engine ---
DAILY_DROP_THRESHOLD=-0.01

# --- Data ---
ASSET_DEFAULT_START=2018-01-01
SP500_TICKER=^GSPC

# --- TwelveData API (REQUIRED for real market data) ---
# Get your FREE API key from https://twelvedata.com/register
# Free tier: 800 API credits/day, 8 requests/minute
TWELVEDATA_API_KEY=your_twelvedata_api_key_here

# API tier configuration (affects rate limiting)
TWELVEDATA_PLAN=free
TWELVEDATA_RATE_LIMIT=8

# Refresh and caching settings
REFRESH_MODE=auto
ENABLE_MARKET_DATA_CACHE=true

# Optional: Skip market data refresh on startup (speeds up deployment)
SKIP_STARTUP_REFRESH=false

# --- MarketAux News API (Optional but recommended) ---
# Get your FREE API key from https://www.marketaux.com/register
# Free tier: 100 requests/day
MARKETAUX_API_KEY=your_marketaux_api_key_here
MARKETAUX_RATE_LIMIT=100
ENABLE_NEWS_CACHE=true
NEWS_REFRESH_INTERVAL=900

# --- Redis Cache (Optional but recommended) ---
# For local development: redis://localhost:6379/0
# For production: Use managed Redis service
REDIS_URL=redis://localhost:6379/0
CACHE_TTL_SECONDS=300
CACHE_TTL_LONG_SECONDS=3600

# --- Google OAuth Configuration ---
GOOGLE_CLIENT_ID=your-google-client-id-here
GOOGLE_CLIENT_SECRET=your-google-client-secret-here
GOOGLE_REDIRECT_URI=http://localhost:3000/auth/callback

# --- Frontend CORS ---
FRONTEND_URL=http://localhost:3000
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# --- Additional Feature Flags ---
ENABLE_WEBSOCKET=true
ENABLE_BACKGROUND_TASKS=true

# --- Deployment ---
PORT=8000
ENVIRONMENT=development
DEBUG=false
LOG_LEVEL=INFO
